const initialState = {
    allActivities: [],
    allCountries: [],
}

const reducer = (state = initialState, {type, payload}) => {
    switch (type) {
        // case value:
            
        //     break;
    
        default:
            return {...state};
    }
}

export default reducer;